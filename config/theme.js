const colors = {
  eerieBlack: `#141618`,
  eerieBlack2: `#111920`,
  neonCarrot: `#FD9D42`,
  wildBlueYonder: `#A1B2BB`,
  charlestonGreen: `#282E30`,
  white: `white`,
  black: `#222`,
};

const light = {
  bg: colors.white,
  float: colors.white,
  accent: colors.black,
};

const dark = {
  bg: colors.black,
  accent: colors.white,
};

const theme = {
  light,
  dark,
  colors,
  width: `900px`,
  maxWidth: `100%`,
  baseFontSize: `10px`,
};

export default theme;
